# Changelog - core-assets

## [Unreleased] - 2026-03-08

### Added
- README del proyecto core-assets.
- CHANGELOG del proyecto core-assets.
- Funciones SCSS: `strip-unit()` y `px-to-rem()`.
- Mixin `focus-visible` para accesibilidad.
- Token bridge `tokens/_elevation.scss`.

### Changed
- Tokens de color a esquema agnostico (`neutral`/`brand`).
- Tokens de spacing/typography/breakpoints convertidos a `rem` mediante `px-to-rem()`.
- Agregacion SCSS principal actualizada para exponer `tokens`, `functions` y `mixins`.
