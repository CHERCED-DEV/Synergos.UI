export interface CartItemInputs {
  config?: string;
  item: string;
  productSku: string;
  quantity: string;
  unitPrice: string;
  updateEndpoint: string;
  theme: string;
  variant: string;
  variantKey: string;
}
