{
  "name": "shared",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "vitals/shared/src",
  "projectType": "library",
  "tags": [
    "scope:shared",
    "framework:agnostic"
  ],
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx tsc -p vitals/shared/tsconfig.json --noEmit"
      }
    }
  }
}
