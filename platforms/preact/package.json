{
  "name": "@synergos/preact",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "La SEGUNDA plataforma (#64). Existe para sostener un elemento publicado, no al revés.",
  "scripts": {
    "build": "node tools/build.mjs",
    "sync:tokens": "node tools/sync-tokens.mjs",
    "sync:tokens:check": "node tools/sync-tokens.mjs --check",
    "test": "node tools/build.mjs && npx --prefix ../.. vitest run --config vitest.config.ts"
  },
  "dependencies": {
    "preact": "^10.27.2"
  },
  "devDependencies": {
    "esbuild": "^0.27.2",
    "sass": "^1.97.1",
    "typescript": "^5.9.3"
  }
}
