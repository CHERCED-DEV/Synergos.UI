// Svelte custom elements auto-register via <svelte:options customElement="...">
// Importing them is enough to trigger registration.

import './elements/accordion/Accordion.svelte';
import './elements/avatar/Avatar.svelte';
