{
  "name": "domains-shop-variant-picker",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "prefix": "sg",
  "sourceRoot": "apps/domains/shop/variant-picker/src",
  "tags": ["scope:domains", "tier:primitive", "type:app", "element:variant-picker", "domain:shop", "framework:angular"],
  "targets": {
    "build": {
      "executor": "@angular/build:application",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/domains/variant-picker",
        "browser": "apps/domains/shop/variant-picker/src/main.ts",
        "index": "apps/domains/shop/variant-picker/src/index.html",
        "tsConfig": "apps/domains/shop/variant-picker/tsconfig.app.json"
      },
      "configurations": {
        "production": { "budgets": [{ "type": "initial", "maximumWarning": "100kb", "maximumError": "200kb" }] }
      }
    },
    "serve": { "executor": "@angular/build:dev-server", "options": { "port": 4266 } },
    "lint": { "executor": "@nx/eslint:lint", "options": { "lintFilePatterns": ["apps/domains/shop/variant-picker/src/**/*.ts"] } }
  }
}
