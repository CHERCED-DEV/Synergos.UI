{
  "name": "domains-shop-cart-item",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "prefix": "sg",
  "sourceRoot": "apps/domains/shop/cart-item/src",
  "tags": ["scope:domains", "tier:composition", "type:app", "element:cart-item", "domain:shop", "framework:angular"],
  "targets": {
    "build": {
      "executor": "@angular/build:application",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/cart-item",
        "browser": "apps/domains/shop/cart-item/src/main.ts",
        "index": "apps/domains/shop/cart-item/src/index.html",
        "tsConfig": "apps/domains/shop/cart-item/tsconfig.app.json"
      },
      "configurations": {
        "production": { "budgets": [{ "type": "initial", "maximumWarning": "150kb", "maximumError": "300kb" }] }
      }
    },
    "serve": { "executor": "@angular/build:dev-server", "options": { "port": 4263 } },
    "lint": { "executor": "@nx/eslint:lint", "options": { "lintFilePatterns": ["apps/domains/shop/cart-item/src/**/*.ts"] } }
  }
}
