{
  "name": "react-quiz-flow",
  "$schema": "../../../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "platforms/react/apps/experiences/quiz-flow/src",
  "tags": [
    "scope:experiences",
    "tier:module",
    "type:app",
    "element:quiz-flow",
    "framework:react"
  ],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/platforms/react/dist/quiz-flow"],
      "options": {
        "command": "npx vite build",
        "cwd": "platforms/react",
        "env": {
          "ELEMENT_NAME": "quiz-flow",
          "ELEMENT_ENTRY": "apps/experiences/quiz-flow/src/main.tsx"
        }
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx vite build --watch",
        "cwd": "platforms/react",
        "env": {
          "ELEMENT_NAME": "quiz-flow",
          "ELEMENT_ENTRY": "apps/experiences/quiz-flow/src/main.tsx"
        }
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx vitest run --config vitest.config.ts",
        "cwd": "platforms/react"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx eslint apps/experiences/quiz-flow/src/",
        "cwd": "platforms/react"
      }
    }
  }
}
