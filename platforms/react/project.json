{
  "name": "react-hero",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "platforms/react/src",
  "projectType": "application",
  "tags": [
    "framework:react",
    "tier:module",
    "element:hero"
  ],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx vite build",
        "cwd": "platforms/react"
      }
    }
  }
}
